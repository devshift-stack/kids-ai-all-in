import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Alanko AI app smoke test', (WidgetTester tester) async {
    // Basic smoke test - app structure tests will be added later
    expect(true, isTrue);
  });
}
